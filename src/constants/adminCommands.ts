/**
 * Names of commands, that can be called by admins
 */
const AdminCommands = [
    "/adminpower",
    "/aidenmode",
    "/aidensilent",
    "/help",
    "/messagelocale",
    "/messagelocalereset",
    "/noemoji",
    "/silentofflocale",
    "/silentofflocalereset",
    "/silentonlocale",
    "/silentonlocalereset",
    "/silent",
] as const;

export default AdminCommands;
