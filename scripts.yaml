allow:
  - net
  - read
  - env

scripts:
  start:
    desc: Starts the bot
    cmd: bot.ts

  dev:
    desc: Starts the bot in watch mode
    cmd: bot.ts
    watch: true

  upgrade:
    desc: Upgrades dependencies
    cmd: deno cache --reload --lock=lock.json ./deps.ts
